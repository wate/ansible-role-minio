MINIO_OPTS=" --address {{ minio_address }} --console-address {{ minio_console_address }}"
MINIO_VOLUMES="{{ '{' + minio_volumes | join(',') + '}' }}"

MINIO_ROOT_USER="{{ minio_root_user }}"
MINIO_ROOT_PASSWORD="{{ minio_root_password }}"
